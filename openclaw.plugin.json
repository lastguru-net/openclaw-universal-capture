{
  "id": "openclaw-universal-capture",
  "kind": "context-engine",
  "name": "OpenClaw Universal Capture",
  "description": "Append-only Markdown conversation capture context engine.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "folder": {
        "type": "string",
        "description": "Workspace-relative folder for daily conversation markdown files."
      },
      "recallFolder": {
        "type": "string",
        "description": "Workspace-relative folder for per-session NDJSON recall files. Defaults to recall."
      },
      "recallTurns": {
        "type": "integer",
        "minimum": 0,
        "description": "Number of recent request-response turns to retain per sessionKey. Defaults to 0, which disables recall."
      },
      "recallMaxBytes": {
        "type": "integer",
        "minimum": 0,
        "description": "Maximum bytes for a per-session recall file and recall tool output. Defaults to 0 for no size limit. The newest stored turn is always retained."
      },
      "timezone": {
        "type": "string",
        "description": "IANA timezone used for daily rollover and entry times. Defaults to the host timezone."
      },
      "rolloverTime": {
        "type": "string",
        "description": "Local HH:MM time when capture switches to the next conversation day. Defaults to 04:00."
      },
      "skipNoReply": {
        "type": "boolean",
        "description": "When true, omit turns whose assistant text is exactly NO_REPLY. Defaults to false, so NO_REPLY marker turns are captured."
      },
      "includeMessageMetadata": {
        "type": "boolean",
        "description": "When true, add inferred agent/surface/channel metadata and senderUsername when present. Defaults to false."
      },
      "stripUntrustedMetadata": {
        "type": "boolean",
        "description": "When true, strip leading OpenClaw untrusted metadata blocks from captured user text. Defaults to true."
      },
      "agents": {
        "type": "string",
        "description": "Optional agent-name filter. Omit or use * for all, use comma-separated values to include, or prefix with ! to exclude."
      },
      "surfaces": {
        "type": "string",
        "description": "Optional surface-name filter. Omit or use * for all, use comma-separated values to include, or prefix with ! to exclude."
      },
      "channels": {
        "type": "string",
        "description": "Optional channel-id filter using the final sessionKey element. Omit or use * for all, use comma-separated values to include, or prefix with ! to exclude."
      }
    },
    "required": []
  },
  "uiHints": {
    "folder": {
      "label": "Capture Folder",
      "placeholder": "conversations",
      "help": "Workspace-relative folder for daily Conversation markdown files."
    },
    "recallFolder": {
      "label": "Recall Folder",
      "placeholder": "recall",
      "help": "Workspace-relative folder for per-session NDJSON recall files."
    },
    "recallTurns": {
      "label": "Recall Turns",
      "placeholder": "0",
      "help": "Number of recent request-response turns retained per sessionKey. Set to 0 to disable recall."
    },
    "recallMaxBytes": {
      "label": "Recall Max Bytes",
      "placeholder": "0",
      "help": "Maximum bytes for stored recall files and recall tool output. Set to 0 for no size limit."
    },
    "timezone": {
      "label": "Rollover Timezone",
      "help": "IANA timezone used for daily file rollover and entry timestamps. Defaults to the host timezone."
    },
    "rolloverTime": {
      "label": "Rollover Time",
      "placeholder": "04:00",
      "help": "Local HH:MM time when capture switches to the next conversation day."
    },
    "skipNoReply": {
      "label": "Skip NO_REPLY",
      "help": "When enabled, NO_REPLY marker turns are omitted from the markdown log instead of being captured like normal assistant text."
    },
    "includeMessageMetadata": {
      "label": "Include Metadata",
      "help": "When enabled, each entry includes agent, surface, channel, and sender username when available."
    },
    "stripUntrustedMetadata": {
      "label": "Strip Untrusted Metadata",
      "help": "When enabled, leading Conversation info and following Sender untrusted metadata blocks are omitted from captured user text."
    },
    "agents": {
      "label": "Agent Filter",
      "placeholder": "home,mini or !main",
      "help": "Capture only listed agent names, exclude listed names with !, or use * / blank for all."
    },
    "surfaces": {
      "label": "Surface Filter",
      "placeholder": "discord or !main",
      "help": "Capture only listed surface names, exclude listed names with !, or use * / blank for all."
    },
    "channels": {
      "label": "Channel Filter",
      "placeholder": "123456789012345678 or !heartbeat",
      "help": "Capture by the final sessionKey element only. Use comma-separated values, ! for exclusion, or * / blank for all."
    }
  },
  "contracts": {
    "tools": ["universal_recall"]
  }
}
