{
  "name": "openclaw-universal-capture",
  "version": "0.7.0",
  "description": "OpenClaw context engine for append-only Markdown conversation capture.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/index.js",
    "dist/index.d.ts",
    "dist/src/",
    "index.ts",
    "src/",
    "openclaw.plugin.json",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "prepare": "npm run build",
    "test": "npm run build && node --test dist/test/*.test.js",
    "release:check": "npm run check-types && npm test && npm pack --dry-run"
  },
  "keywords": [
    "openclaw",
    "conversation",
    "capture",
    "context-engine"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22.22.3 <23 || >=24.15.0 <25 || >=25.9.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lastguru-net/openclaw-universal-capture"
  },
  "peerDependencies": {
    "openclaw": ">=2026.8.1-beta.2 <2026.9.0-0"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "openclaw": "2026.8.1-beta.2",
    "typescript": "^6.0.3"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ],
    "runtimeExtensions": [
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.8.1-beta.2 <2026.9.0-0"
    },
    "install": {
      "minHostVersion": ">=2026.8.1-beta.2"
    },
    "build": {
      "openclawVersion": "2026.8.1-beta.2"
    }
  }
}
